Distribution.py:
	- only analyze the given folder
	- remove the hole part of csv construction to another place

Augmentation.py:
	- do not create a new augmented_directory by default so that logic need to go
	- only create 6 files from image, with the same name, it dosent have the option to make it from a directory
	- it needs to externalize the part where split by evaluation, test, train
	- need to display the images done for the augmented img

Transformation.py:
	- need to add -h flag

train.py:
	- this one will:
		- augment the directory with augmentation logic
		- split into evaluation, test, and train
		- build the csv from a given directory
		- train the model based on that csv (done)
		- save the models in a folder

./predict.py
	- changes needed:
		- only takes the image jpg to predict
